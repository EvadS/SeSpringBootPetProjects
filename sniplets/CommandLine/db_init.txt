



@SpringBootApplication
public class DemoApplication {

    public static void main(String[] args) {
        SpringApplication.run(DemoApplication.class, args);
    }

    @Bean
    ApplicationRunner init(CarRepository repository) {
        return args -> {
            Stream.of("Ferrari", "Jaguar", "Porsche", "Lamborghini", "Bugatti",
                      "AMC Gremlin", "Triumph Stag", "Ford Pinto", "Yugo GV").forEach(name -> {
                Car car = new Car();
                car.setName(name);
                repository.save(car);
            });
            repository.findAll().forEach(System.out::println);
        };
    }
}

//------------------------------------------------------
// entity
@Entity
@Data
@NoArgsConstructor
public class Car {
    @Id @GeneratedValue
    private Long id;
    private @NonNull String name;
}


создал докер с установленный ssh

сервис получилось запустить, хотя на данном этапе есть ошибки с  Бд
если я правильно понял то были дополнительные действия по настроке
я вижу что не все таблицы создается через ORM
и не хватает нескольких скриптов по настроке например герерация пароля при помощи DBMS_CRYPTO где не хватает кредов
сейчас над этим работаю
К  борд в джире не доступен.
