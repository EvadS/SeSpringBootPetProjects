# Comma-delimited list of host:port pairs to use for establishing the initial connection
spring.kafka.consumer.bootstrap-servers= localhost:9092
# specifies a unique string that identifies the consumer group this consumer belongs to
spring.kafka.consumer.group-id= group-id
spring.kafka.consumer.auto-offset-reset= earliest
spring.kafka.consumer.key-deserializer= org.apache.kafka.common.serialization.StringDeserializer
# the following Consumer property to convert JSON into Java object
spring.kafka.consumer.value-deserializer=org.springframework.kafka.support.serializer.JsonDeserializer
spring.kafka.consumer.properties.spring.json.trusted.packages=*

spring.kafka.producer.bootstrap-servers= localhost:9092
spring.kafka.producer.key-serializer= org.apache.kafka.common.serialization.StringSerializer
# the following Producer property to convert Java object into JSON:
spring.kafka.producer.value-serializer= org.springframework.kafka.support.serializer.JsonSerializer

spring.kafka.consumer.topic=topic_demo2
spring.kafka.consumer.groupid=1